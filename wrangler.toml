# Cloudflare Pages: static deploy (build happens in CI).
# Local preview: cd app && pnpm build && npx wrangler pages dev ../app/dist --config ../wrangler.toml
name = "react-cloudflare-template"
compatibility_date = "2026-08-07"
pages_build_output_dir = "app/dist"
